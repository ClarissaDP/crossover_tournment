

def rearrange(elements):

    size = elements[0] + 1
    binaries = [size]

    for i in range(1, size):
        binary = bin(elements[i]).
        binaries.append (bin(elements[i]))

    print binaries

rearrange([3,1,2,3])

